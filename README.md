# 情侣专属爱情空间

一个基于 Next.js 和 Supabase 的现代化爱情纪念网站，支持后台管理和数据同步。

## 功能特性

- ✨ **响应式设计** - 适配各种设备屏幕
- 📱 **后台管理系统** - 完整的CRUD操作界面
- 🔐 **用户认证** - Supabase Auth 安全登录
- 💾 **数据持久化** - Supabase PostgreSQL 数据库
- 🎵 **音乐播放** - 可自定义背景音乐
- 🖼️ **图片上传** - 支持本地文件和网络URL
- ⏰ **恋爱计时器** - 精确到秒的时间计算
- 🚀 **Netlify部署** - 一键部署到生产环境

## 技术栈

- **前端**: Next.js 14, React 18, TypeScript, Tailwind CSS
- **后端**: Next.js API Routes, Supabase
- **数据库**: PostgreSQL (Supabase)
- **认证**: Supabase Auth
- **存储**: Supabase Storage
- **部署**: Netlify

## 快速开始

### 1. 环境准备

```bash
# 克隆项目
git clone <repository-url>
cd love-hansenning

# 安装依赖
npm install
```

### 2. 数据库配置

1. 在 [Supabase](https://supabase.com) 创建新项目
2. 执行 `database-schema.sql` 中的SQL语句创建表结构
3. 在 Supabase 设置中启用 Authentication
4. 创建用户用于后台登录

### 3. 环境变量配置

复制 `.env.local.example` 为 `.env.local` 并填写配置：

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
NEXT_PUBLIC_SITE_URL=https://your-site.netlify.app
```

### 4. 本地开发

```bash
# 启动开发服务器
npm run dev

# 访问网站
http://localhost:3000

# 访问后台管理
http://localhost:3000/admin
```

### 5. 部署到 Netlify

1. 将代码推送到 GitHub 仓库
2. 在 [Netlify](https://netlify.com) 连接仓库
3. 设置构建命令：`npm run build`
4. 设置发布目录：`.next`
5. 配置环境变量
6. 点击部署

## 项目结构

```
├── app/                    # Next.js App Router
│   ├── admin/             # 后台管理页面
│   ├── api/               # API 路由
│   ├── globals.css        # 全局样式
│   ├── layout.tsx         # 根布局
│   └── page.tsx           # 首页
├── components/            # React 组件
│   ├── AdminDashboard.tsx # 后台管理面板
│   ├── LoveTimer.tsx      # 恋爱计时器
│   └── ...               # 其他组件
├── lib/                   # 工具函数
│   └── supabase.ts        # Supabase 客户端
├── public/               # 静态资源
└── ...
```

## 数据库表结构

- `website_config` - 网站基础配置
- `anniversaries` - 纪念日数据
- `wishlist` - 愿望清单
- `explore_locations` - 探索地点
- `photo_gallery` - 照片墙

## API 接口

- `GET /api/config` - 获取网站配置
- `PUT /api/config` - 更新网站配置
- `GET /api/anniversaries` - 获取纪念日列表
- `POST /api/anniversaries` - 创建纪念日
- ... 更多接口详见代码

## 后台管理功能

- **网站配置**: 修改情侣名字、纪念日期、背景音乐等
- **纪念日管理**: 添加、编辑、删除纪念日
- **愿望清单**: 管理共同愿望和目标
- **探索地点**: 记录想去的地方
- **照片墙**: 上传和管理照片

## 开发说明

### 添加新功能模块

1. 在数据库中创建对应的表
2. 在 `lib/supabase.ts` 中添加类型定义和API函数
3. 创建对应的 API 路由
4. 创建前端组件
5. 集成到后台管理界面

### 自定义样式

项目使用 Tailwind CSS，可以通过修改 `tailwind.config.js` 来自定义主题和样式。

## 部署说明

### Netlify 部署流程

1. **连接仓库**: 在 Netlify 中连接你的 GitHub 仓库
2. **构建设置**: 
   - Build command: `npm run build`
   - Publish directory: `.next`
3. **环境变量**: 在 Netlify 设置中添加环境变量
4. **域名配置**: 配置自定义域名 (可选)

### 环境变量要求

在 Netlify 的环境变量设置中需要配置：

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `NEXT_PUBLIC_SITE_URL`

## 许可证

MIT License

## 联系方式

如有问题请联系项目维护者。